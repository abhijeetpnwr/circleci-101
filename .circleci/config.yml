version: 2
jobs:
  build:
    docker:
      - image: centos
    steps:
    
      - checkout

      - run:
          name: Greeting
          command: echo Hello, world.

      - run:
          name: Print the Current Time
          command: date
      -run
        name: Build Python Package
        command: python setup.py install --single-version-externally-managed --record=/tmp/record.txt
